{
  "name": "ai-drama-supplier-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --import ./src/network-denial.mjs --test src/*.test.ts"
  },
  "dependencies": {
    "esbuild": "0.25.12"
  }
}
