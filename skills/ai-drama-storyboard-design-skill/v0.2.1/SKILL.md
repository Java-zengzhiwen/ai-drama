# AI Drama Storyboard Design Skill v0.2.0

Return only Storyboard Canonical JSON matching `storyboard-canonical-v1`.

Markdown is not authoritative in this profile. Runtime renders Markdown from
canonical JSON when a derived view is needed.
