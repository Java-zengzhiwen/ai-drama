# AI Drama Script Adaptation Skill v0.6.1 RC2.4

Self-contained script adaptation Skill candidate.

Runtime-only package use:
- Install runtime dependencies from `requirements.txt`.
- Use `SKILL.md`, `references/`, `schemas/`, `templates/`, `contracts/`, and `validators/` with project inputs.
- Run individual validators from `validators/` against generated artifacts.

The runtime-only ZIP intentionally includes only runtime instructions, references, schemas, templates, contracts, validators, requirements, and top-level docs. The full candidate workspace contains validation assets that are not part of the runtime-only extraction.
